<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Weather App</title>
    <style>
      
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="compass-icon"><img src="/icons/icon.svg" alt="img weather"></div>
        </header>
        <div class="weather-title">
            <h1>Simply Weather</h1>
            <p>View the weather for your City</p>
        </div>
        <form action="/" method="post">
            <input type="text" name="city" class="ghost-input" placeholder="Enter a city">
        </form>
        <div class="result">
            <% if(temp !==null) {%>
                <h1 class="temp"><%= temp-273 %></h1>
            <%}  %>

           
        <div class="icons-des">
            <% if(icon !==null) {%>
              <img class="icon" src="http://openweathermap.org/img/w/<%= icon %>.png" alt="">
            <% } %>  

            <% if(icon !==null) {%>
              <h3><%= des %></h3>
            <% } %>
        </div>
        <% if(city !==null) { %>
            <p class="city" style="color:<%=color%> "> <%= city %></p>
           <% } %>
        </div>
    </div>
</body>
</html> 